# ── Her actual board data. Client names, amounts, private notes. NEVER commit. ──
# The repo backs up CODE. The board itself lives only on this Mac + its backups.
pm_state.json
pm_state.json.bak
pm_state.json.bak_*
pm_state.json.bak*
*.bak
*.bak_*

# The permanent record of finished work, written by roll_forward → archive_finished.
# Carries her `did` and `note` verbatim — the most sensitive content in the repo,
# because it is the narrative of what she actually did, per client. Added
# 2026-07-29, the same session the archive started being written: it was
# untracked-but-not-ignored, one `git add -A` away from GitHub.
history/

# Briefings are built from Gmail, Slack and BuilderTrend and quote clients,
# subs, amounts and Rafael's messages verbatim. Same reasoning as the board.
briefings/

*.tmp
.pm_state.lock
logs/
__pycache__/
.pytest_cache/
ctx/

# The raw transcript of the last sweep, kept on failure/timeout so a bad run is
# debuggable instead of silent. It is the model's verbatim read of Gmail, Slack and
# BuilderTrend, so a real run puts client names, amounts and Rafael's messages in it.
# Today's copy happens to be 88 bytes; that is timing, not safety. Added 2026-08-03,
# found untracked-but-not-ignored at the Monday re-ground -- same shape as history/.
last_sweep_output.txt
